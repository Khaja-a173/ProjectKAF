{
  "name": "@projectkaf/db",
  "version": "1.0.0",
  "description": "ProjectKAF Database Layer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1"
  },
  "devDependencies": {
    "prisma": "^5.3.1",
    "typescript": "^5.1.6",
    "@types/node": "^20.5.0"
  }
}